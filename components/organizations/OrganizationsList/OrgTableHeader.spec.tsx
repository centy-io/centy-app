import { describe, it, expect } from 'vitest'
import { OrgTableHeader } from './OrgTableHeader'

describe('OrgTableHeader', () => {
  it('is a valid component', () => {
    expect(typeof OrgTableHeader).toBe('function')
  })
})
